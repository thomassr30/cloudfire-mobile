import {View, Text} from 'react-native';
import React from 'react';

const RecoverPassScreen = () => {
  return (
    <View>
      <Text>RecoverPassScreen</Text>
    </View>
  );
};

export default React.memo(RecoverPassScreen);
